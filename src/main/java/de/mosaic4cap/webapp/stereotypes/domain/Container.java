package de.mosaic4cap.webapp.stereotypes.domain;

import org.apache.log4j.Logger;

/**
 * Created by Lobedan on 22.09.2014.
 *
 * Container for objects which contains more
 * than one object
 * that means they store multiple objects inside themselves
 */
public abstract class Container {
    private static final Logger LOGGER = Logger.getLogger(Container.class);
}
